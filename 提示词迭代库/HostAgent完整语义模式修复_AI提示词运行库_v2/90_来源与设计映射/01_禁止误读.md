# 禁止误读

> 状态：**ACTIVE**  
> 运行库版本：**v2.0**  
> 生成日期：**2026-09-07**

1. FULL 不需要 API Key，但仍需要合格 Executor。
2. 当前聊天有模型 ≠ Daemon 能主动 invoke 它。
3. Host Agent 不是 Canonical owner。
4. Adapter 是桥，不是 Graph policy。
5. Tier B不能假执行。
6. HEURISTIC 仍保留。
7. External API仍可用，只是 optional。
8. Prompt Profile跨 Host共用。
9. Host offline 不影响 iPad/Store。
