# 通用 Host Agent 完整语义模式修复 AI 提示词运行库

版本：**v2.0**

用于直接指导 AI 修改现有阅读系统，使 Codex / ZCode / WorkBuddy / 其他合格 Agent 都可成为 `HOST_AGENT` Semantic Executor。

核心目标：

```text
FULL Semantic Mode ≠ API Key Required
FULL Semantic Mode = Eligible Semantic Executor Required
```

从 `00_启动/00_START_HERE.md` 开始。
