# 最新设计优先级与历史资料使用规则

> 状态：**CANONICAL-META**  
> 设计基线：**v1.0-canonical**  
> 整理日期：**2026-09-07**

本目录用于防止后续 AI 从旧聊天/旧设计稿恢复已经被覆盖的规则。旧资料只可作为讨论历史，不能直接复制进实现。

必须记住：
1. `scope_summary` 已统一升级为 `Anchor Summary`；
2. Focus 已重新定义为 iPad ViewState 的派生状态；
3. CREATE 页面跳转改为 conditional；
4. Pencil 改为落笔即写；
5. Interest 已降低精确 dwell-time 权重；
6. Working Conversation Memory 采用事件边界，而不是固定分钟过期；
7. v1 明确禁止跨 Graph 自动机制；
8. Mac 最终成果不是单个超级 system prompt，而是 Prompt Runtime Library/Profiles + 确定性运行层。

若历史聊天与本包冲突，必须以本包 CANONICAL 文档为准。
