# 端到端主流程

> 状态：**CANONICAL**  
> 设计基线：**v1.0-canonical**  
> 整理日期：**2026-09-07**

## Fresh Reading
```text
Scheduler chooses fresh G
→ Mac sync CONTENT
→ GRAPH_PUSH(FRESH)
→ iPad BLOCK_VIEW
→ VIEW_COMMITTED
→ Mac Focus=Block
```
用户 QA：freeze TurnContext → Responder context/answer → Curator context → NO_OP/CREATE。
CREATE B：Mac commit B → GraphPatch → iPad durable apply → conditional nav → 若 basis 仍有效则 NODE_VIEW(B) → VIEW_COMMITTED → Focus=B。

## EXTEND
Node A QA → TurnContext(A) → Curator EXTEND → append Segment/update Summary → Patch。iPad old geometry 不动、底部 append、viewport 保持、无导航。

## END
END_CURRENT_GRAPH → close Episode → Interest → Timing → Scheduler。Continuation → Fresh Block；Temporal → old Graph Topology。

## Offline Pencil
Mac disconnect → iPad OFFLINE_CACHED → Pencil local durable → annotation outbox → reconnect → auth/sync → upload snapshot → ACK。

## Stale CREATE
Q 在 A/view10 提交 → 用户去 C/view11 → CREATE B 完成 → B sync → conditional nav basis10 → STALE → 用户留 C。此场景必须通过。
