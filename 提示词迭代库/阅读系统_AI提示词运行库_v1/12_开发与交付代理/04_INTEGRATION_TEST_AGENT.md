# Integration / Failure Injection Agent

> Prompt ID：`dev.integration_test`  
> 版本：`runtime-v1.0`  
> 模式：**IMPLEMENTATION**  
> 日期：**2026-09-07**

建立可自动和真机执行的集成测试：

主链：Fresh → QA → EXTEND → QA → CREATE → Pencil → Topology → END/NEXT → Temporal。

失败注入：
- duplicate patch；
- socket drop；
- Mac restart；
- iPad kill；
- revision mismatch；
- commit response lost；
- stale conditional nav；
- annotation conflict；
- derived index failure。

任何 Ink 丢失/错位、duplicate Node/Segment、wrong Focus、stale nav 抢页均为 release blocker。
