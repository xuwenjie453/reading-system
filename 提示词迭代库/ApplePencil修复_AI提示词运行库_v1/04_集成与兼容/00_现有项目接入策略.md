# 现有项目接入策略 Prompt

> 状态：**ACTIVE**  
> 运行库版本：**v1.0**  
> 生成日期：**2026-09-07**

你是在现有仓库中修改，不是新建 Demo。

先识别当前：
- Reader Controller / SwiftUI wrappers；
- PKCanvasView owner；
- annotation persistence；
- graph EXTEND 更新；
- app state；
- current toolbar code。

尽量增量重构：
- 保持已存在协议与 Store；
- 替换危险 geometry；
- 建立清晰 PencilCanvasController / InkToolController；
- 不无必要大改整个 app navigation。

每次 commit 聚焦一个职责。
