# Reader / Canvas 几何审计 Prompt

> 状态：**ACTIVE**  
> 运行库版本：**v1.0**  
> 生成日期：**2026-09-07**

对当前 Reader 做完整几何审计。

运行时记录至少：

```text
ReaderViewportScrollView.frame/bounds
ReaderViewportScrollView.zoomScale
CanonicalDocumentView.frame/bounds
CanonicalBodyView.frame/bounds
TextSurface.frame/bounds/transform
PKCanvasView.frame/bounds/transform
PKCanvasView.contentSize
PKCanvasView.contentOffset
PKCanvasView.contentInset
PKCanvasView.zoomScale
所有 ancestor transforms
layoutProfileID
layoutEpoch
```

在以下时刻打印对比：
1. Pencil 远离；
2. hover begin；
3. hover active；
4. stroke begin；
5. stroke end；
6. hover end。

如果 hover 期间任何 geometry / transform 变化，标记为高风险。

最后给出：
```text
Screen point
→ Viewport
→ CanonicalBody
→ PKCanvasView
```
的实际转换链，并指出哪一步重复 scale/offset。
