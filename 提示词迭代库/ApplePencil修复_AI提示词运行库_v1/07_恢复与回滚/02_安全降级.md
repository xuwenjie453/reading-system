# 安全降级 Prompt

> 状态：**ACTIVE**  
> 运行库版本：**v1.0**  
> 生成日期：**2026-09-07**

如果某 legacy Entity 无法安全迁移到新 Layout：

正确降级：
```text
该 Entity 使用 Legacy LayoutProfile
+
正常 Pencil
+
正常 sync
```

而不是：
- 禁止阅读；
- 丢掉笔迹；
- 强制重排；
- 显示错误 transform。

新 Entity 仍可用新的字体/margin profile。
